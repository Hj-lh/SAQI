# System prerequisites (install once via apt):
#   sudo apt install -y swig liblgpio-dev python3-dev python3-venv

fastapi
uvicorn[standard]
opencv-python-headless
ultralytics
gpiozero
lgpio
python-dotenv
pyserial
requests

# Status LEDs (WS2812 on GPIO18). NOTE: rpi_ws281x usually needs root —
# run the server with sudo, or the LED panel self-disables (system still runs).
rpi_ws281x
adafruit-circuitpython-neopixel